import { h } from 'hyperapp';
import './Loader.css';

const Loader = () => (
  <div class="loader">
    <div class="loader__first"/>
    <div class="loader__second"/>
  </div>
);

export default Loader;
